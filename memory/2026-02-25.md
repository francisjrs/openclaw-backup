# 2026-02-25 Daily Notes

## Browser Setup — Bot Server
- Installed Playwright Chromium on bot-server (`npx playwright install chromium`)
- Created `openclaw-browser.service` systemd service — runs Chromium headless on port 18800 CDP
- `openclaw-node.service` now depends on browser service
- Removed socat relay sidecar from docker-compose
- Browser is WORKING: `profile=openclaw, target=node, node=bot-server`
- Config: `/home/framos/.openclaw/openclaw.json` → executablePath = Playwright Chromium

## Node Host Fixes
- Fixed exec allowlist on bot-server: added cat, ls, grep, wc, find, head, tail
- Added DISPLAY=:10 and XAUTHORITY=/home/framos/.Xauthority to openclaw-node service drop-in
- Drop-in at: `/home/framos/.config/systemd/user/openclaw-node.service.d/display.conf`

## CLAUDE.md & memory/openclaw.md Updated
- Both files already had the full OpenClaw ops reference from the spec Franco shared
- No additional changes needed

## Skills Review — ClawHub
- Reviewed skills at clawhub.ai/skills
- ✅ Summarize — HIGH CONFIDENCE, instruction-only
- ✅ Github — HIGH CONFIDENCE, instruction-only (`gh` CLI)
- ✅ self-improving-agent — safe, writes markdown to .learnings/
- 🚫 Find Skills — SUSPICIOUS flagged by OpenClaw
- ⚠️ Brave Search — not needed, already have web_search via Brave API
- ⚠️ Notion — needs API token

## Tool Usage Rules (Franco confirmed)
- 🔍 web_search — quick lookups, news, facts, URLs (Brave Search API). Gives titles, URLs, snippets.
- 🌐 web_fetch — fetch specific URL, extract readable content (HTML→text/markdown). Use when I know exactly where to go.
- 🖥️ browser — real browser (Chromium). For interaction: clicks, forms, screenshots, JS-heavy sites, auth.

## Pending
- Install: Summarize, Github, self-improving-agent skills (Franco approved)
